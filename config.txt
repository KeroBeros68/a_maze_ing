# Maze width (number of cells)
WIDTH=41
# Maze height
HEIGHT=18
# Entry coordinates (x,y)
ENTRY=[0, 0]
# Exit coordinates (x,y)
EXIT=[20, 16]
# Output filename
OUTPUT_FILE=maze.txt
# Is the maze perfect?
PERFECT=true
# Maze generation algorithm (backtracking, etc.)
ALGORITHM=backtracking
# Seed
# SEED=4224522
# Display "basic", "tty", "mlx"
DISPLAY_MODE=tty
# Generation mode: 'normal' (returns final maze) or 'animated' (generator)
MODE_GEN=animated
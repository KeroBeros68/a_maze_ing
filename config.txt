# Maze width (number of cells)
WIDTH=30
# Maze height
HEIGHT=18
# Entry coordinates (x,y)
ENTRY=[1, 1]
# Exit coordinates (x,y)
EXIT=[2,2]
# Output filename
OUTPUT_FILE=maze.txt
# Is the maze perfect?
PERFECT=false
# Maze generation algorithm ("backtracking" or "prim")
ALGORITHM=prim
# Seed
SEED=BENJAMINCESTLEGOAT
# Display "basic", "tty"
DISPLAY_MODE=tty
# Generation mode: 'static' (returns final maze) or 'animated' (generator)
MODE_GEN=static
# Stamp design type: "42vanilla", "ok", "42custom"
STAMP_TYPE=42custom